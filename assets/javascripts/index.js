// Generated by CoffeeScript 1.3.3
(function() {

  $(document).ready(function() {
    var canvas, contentManager, isCanvasSupported, stage;
    isCanvasSupported = function() {
      var elem;
      elem = document.createElement('canvas');
      return !!(elem.getContext && elem.getContext('2d'));
    };
    if (!isCanvasSupported()) {
      return alert("あなたのブラウザはCanvasが使えないです");
    }
    canvas = $('#c')[0];
    stage = new createjs.Stage(canvas);
    contentManager = new ContentManager(stage, canvas.width, canvas.height, function() {
      var platform;
      platform = new GamePlatform(stage, canvas.width, canvas.height, this);
      return platform.startGame();
    });
    return contentManager.startDownload();
  });

}).call(this);
